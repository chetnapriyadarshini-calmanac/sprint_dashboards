# Core (dashboards + retros)
pandas
requests
openpyxl

# Needed only when the capacity workbook is a Google Sheet URL:
#   - OAuth "read as you" (current setup) uses all three, incl. google-auth-oauthlib
#   - service account uses google-api-python-client + google-auth
# Harmless to install if you use the committed .xlsx or a public link instead.
google-api-python-client
google-auth
google-auth-oauthlib
